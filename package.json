{
  "name": "ephemeral-proof-bound-key",
  "version": "1.0.0",
  "description": "This repository hosts the reference documentation, specs, and future prototypes for **E-Key**, an ephemeral, proof-bound capability key designed to replace long-lived API keys and bearer tokens. E-Key combines short-lived JWTs, DPoP/mTLS proof of possession, and capability-scoped permissions to deliver a \"one request, one key\" security model.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:conformance": "vitest run tests/conformance.test.ts",
    "lint": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NishizukaKoichi/ephemeral-proof-bound-key.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/NishizukaKoichi/ephemeral-proof-bound-key/issues"
  },
  "homepage": "https://github.com/NishizukaKoichi/ephemeral-proof-bound-key#readme",
  "dependencies": {
    "fastify": "^5.6.2",
    "jose": "^6.1.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
